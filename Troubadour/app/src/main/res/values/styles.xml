<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Troubadour_1" parent="Theme.AppCompat.NoActionBar">
        <item name="android:colorForeground">@color/troubadour_dark</item>
        <item name="android:textColor">@android:color/primary_text_dark</item>
        <item name="android:itemBackground">@color/troubadour_dark</item>
    </style>

    <style name="Troubadour_1.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Troubadour_1.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
